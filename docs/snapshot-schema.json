{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/stSoftwareAU/NEAT-AI/blob/Develop/docs/snapshot-schema.json",
  "title": "NEAT-AI Creature Export",
  "description": "Schema for the NEAT-AI creature export (snapshot) format. This format is used for serialising creatures to JSON for persistence, visualisation (e.g. NEAT-AI-Explore), and inter-process communication. Validate with: import Ajv from 'ajv'; const ajv = new Ajv(); const validate = ajv.compile(schema); validate(snapshot);",
  "type": "object",
  "required": ["neurons", "synapses", "input", "output"],
  "additionalProperties": false,
  "properties": {
    "semanticVersion": {
      "type": "string",
      "description": "Semantic version of the creature format (e.g. '1.0.0'). Used for migration and upgrade logic."
    },
    "forwardOnly": {
      "type": "boolean",
      "description": "When true, indicates a feed-forward topology with no recurrent (self-loop or backward) connections."
    },
    "input": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of input neurons in the creature. Input neurons are implicit and not listed in the neurons array."
    },
    "output": {
      "type": "integer",
      "minimum": 1,
      "description": "Number of output neurons in the creature."
    },
    "neurons": {
      "type": "array",
      "description": "Array of non-input neurons (hidden, output, and constant). Input neurons are implicit based on the 'input' count.",
      "items": {
        "$ref": "#/$defs/neuron"
      }
    },
    "synapses": {
      "type": "array",
      "description": "Array of connections between neurons, each carrying a weight.",
      "items": {
        "$ref": "#/$defs/synapse"
      }
    },
    "tags": {
      "type": "array",
      "description": "Optional metadata tags attached to the creature (e.g. error, score, training approach).",
      "items": {
        "$ref": "#/$defs/tag"
      }
    },
    "memetic": {
      "$ref": "#/$defs/memetic",
      "description": "Optional memetic evolution state tracking weights, biases, and ancestry across generations."
    },
    "hyperparameters": {
      "$ref": "#/$defs/hyperparameters",
      "description": "Optional per-creature evolvable hyperparameters (Issue #1863). These evolve alongside topology and weights."
    }
  },
  "$defs": {
    "neuron": {
      "type": "object",
      "description": "A non-input neuron in the creature network.",
      "required": ["type", "uuid", "bias"],
      "additionalProperties": false,
      "properties": {
        "type": {
          "type": "string",
          "enum": ["hidden", "output", "constant"],
          "description": "The role of this neuron: 'hidden' for intermediate processing, 'output' for final results, 'constant' for fixed-value bias neurons."
        },
        "uuid": {
          "type": "string",
          "description": "Unique identifier for the neuron. Output neurons use 'output-N' format; hidden/constant neurons use UUIDs or descriptive names."
        },
        "id": {
          "type": "integer",
          "description": "Runtime integer neuron ID (Issue #1958). Not present in external exports (Issue #2054); use uuid instead. Only present in internal JSON for backward compatibility when loading legacy data."
        },
        "bias": {
          "type": "number",
          "description": "Bias value added to the neuron's weighted input sum before the activation function is applied."
        },
        "squash": {
          "type": "string",
          "description": "Activation (squash) function name applied to this neuron (e.g. 'LOGISTIC', 'ReLU6', 'TANH', 'IF', 'GAUSSIAN', 'SELU'). Omitted when using the default activation."
        },
        "frozen": {
          "type": "boolean",
          "description": "When true, this neuron's bias is frozen and will not be modified by backpropagation or mutation (Issue #1861). Used for transfer learning."
        },
        "tags": {
          "type": "array",
          "description": "Optional metadata tags attached to this neuron.",
          "items": {
            "$ref": "#/$defs/tag"
          }
        }
      }
    },
    "synapse": {
      "type": "object",
      "description": "A weighted connection between two neurons in the creature network.",
      "required": ["fromUUID", "toUUID", "weight"],
      "additionalProperties": false,
      "properties": {
        "fromUUID": {
          "type": "string",
          "description": "UUID of the source (pre-synaptic) neuron. Input neurons use 'input-N' format."
        },
        "toUUID": {
          "type": "string",
          "description": "UUID of the destination (post-synaptic) neuron."
        },
        "fromId": {
          "type": "integer",
          "description": "Runtime integer ID of the source neuron (Issue #1958). Not present in external exports (Issue #2054); use fromUUID instead. Only present in internal JSON or legacy data."
        },
        "toId": {
          "type": "integer",
          "description": "Runtime integer ID of the destination neuron (Issue #1958). Not present in external exports (Issue #2054); use toUUID instead. Only present in internal JSON or legacy data."
        },
        "weight": {
          "type": "number",
          "description": "Connection weight multiplied with the source neuron's output."
        },
        "type": {
          "type": "string",
          "enum": ["positive", "negative", "condition"],
          "description": "Optional constraint on the synapse: 'positive' clamps weight >= 0, 'negative' clamps weight <= 0, 'condition' marks a conditional input for IF-type neurons."
        },
        "frozen": {
          "type": "boolean",
          "description": "When true, this synapse's weight is frozen and will not be modified by backpropagation or mutation (Issue #1861). Used for transfer learning."
        },
        "tags": {
          "type": "array",
          "description": "Optional metadata tags attached to this synapse.",
          "items": {
            "$ref": "#/$defs/tag"
          }
        }
      }
    },
    "tag": {
      "type": "object",
      "description": "A name-value metadata pair used to annotate creatures, neurons, or synapses.",
      "required": ["name", "value"],
      "additionalProperties": false,
      "properties": {
        "name": {
          "type": "string",
          "description": "Tag identifier (e.g. 'error', 'score', 'approach', 'trainID')."
        },
        "value": {
          "type": "string",
          "description": "Tag value as a string. Numeric values are stored as their string representation."
        }
      }
    },
    "memeticWeight": {
      "type": "object",
      "description": "A single weight record in the memetic evolution state.",
      "required": ["weight"],
      "additionalProperties": false,
      "properties": {
        "toUUID": {
          "type": "string",
          "description": "UUID of the destination neuron for this weight. Present in legacy exports."
        },
        "toId": {
          "type": "integer",
          "description": "Integer ID of the destination neuron (Issue #1958). Present in new exports."
        },
        "weight": {
          "type": "number",
          "description": "The weight value at this generation."
        }
      }
    },
    "memeticAncestorSnapshot": {
      "type": "object",
      "description": "A snapshot of memetic state from a previous generation, used to track evolution of weights and biases.",
      "required": ["generation", "weights", "biases", "score"],
      "additionalProperties": false,
      "properties": {
        "generation": {
          "type": "integer",
          "minimum": 0,
          "description": "The generation number when this snapshot was taken."
        },
        "weights": {
          "$ref": "#/$defs/memeticWeights",
          "description": "Weight state at this generation, keyed by source neuron UUID or integer ID."
        },
        "biases": {
          "$ref": "#/$defs/memeticBiases",
          "description": "Bias state at this generation, keyed by neuron UUID or integer ID."
        },
        "score": {
          "type": "number",
          "description": "Fitness score at this generation."
        }
      }
    },
    "memeticWeights": {
      "type": "object",
      "description": "Map of source neuron identifier to an array of weight records. Keys may be UUID strings (legacy) or integer ID strings (new format). In JSON, all keys are strings regardless of whether the ID is conceptually numeric.",
      "additionalProperties": {
        "type": "array",
        "items": {
          "$ref": "#/$defs/memeticWeight"
        }
      }
    },
    "memeticBiases": {
      "type": "object",
      "description": "Map of neuron identifier to its bias value at a given generation. Keys may be UUID strings (legacy) or integer ID strings (new format). In JSON, all keys are strings regardless of whether the ID is conceptually numeric.",
      "additionalProperties": {
        "type": "number"
      }
    },
    "memetic": {
      "type": "object",
      "description": "Memetic evolution state tracking the current generation's weights and biases, plus an optional ancestry buffer.",
      "required": ["generation", "weights", "biases", "score"],
      "additionalProperties": false,
      "properties": {
        "generation": {
          "type": "integer",
          "minimum": 0,
          "description": "Current generation number for memetic evolution."
        },
        "weights": {
          "$ref": "#/$defs/memeticWeights",
          "description": "Current weight state, keyed by source neuron UUID or integer ID."
        },
        "biases": {
          "$ref": "#/$defs/memeticBiases",
          "description": "Current bias state, keyed by neuron UUID or integer ID."
        },
        "score": {
          "type": "number",
          "description": "Current fitness score."
        },
        "ancestry": {
          "type": "array",
          "description": "Optional circular buffer of ancestor snapshots, ordered from most recent to oldest. Limited to 3 entries by default.",
          "maxItems": 3,
          "items": {
            "$ref": "#/$defs/memeticAncestorSnapshot"
          }
        }
      }
    },
    "hyperparameters": {
      "type": "object",
      "description": "Per-creature evolvable hyperparameters (Issue #1863). These values evolve alongside topology and weights via mutation and crossover.",
      "additionalProperties": false,
      "properties": {
        "learningRate": {
          "type": "number",
          "description": "Per-creature learning rate for backpropagation."
        },
        "addNeuronRate": {
          "type": "number",
          "description": "Per-creature probability of adding a neuron during mutation. Range: [0, 1]."
        },
        "addConnectionRate": {
          "type": "number",
          "description": "Per-creature probability of adding a connection during mutation. Range: [0, 1]."
        },
        "weightPerturbationScale": {
          "type": "number",
          "description": "Per-creature weight perturbation magnitude scaling factor."
        },
        "l1RegularisationStrength": {
          "type": "number",
          "description": "Per-creature L1 regularisation strength for backpropagation."
        },
        "l2RegularisationStrength": {
          "type": "number",
          "description": "Per-creature L2 regularisation strength for backpropagation."
        }
      }
    }
  }
}
