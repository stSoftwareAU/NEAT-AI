# Cargo workspace for NEAT-AI Rust crates
# Issue #1964 - Extract shared Rust library crate from wasm_activation
[workspace]
members = ["neat-core", "wasm_activation"]
resolver = "2"

[profile.release]
opt-level = 3
lto = true

[profile.release.package."*"]
opt-level = 3
